function isEven(value) {
    if (value % 2 === 0) {
        return true;
    }
    else
        return false;
    }
}

O(1) linear time? two constant time actions, mod division, and comparison. regardless of size of input.

